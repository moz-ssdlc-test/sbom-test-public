name: SSDLC Actions

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions: {}

jobs:
  code-security-analysis:
    permissions:
      security-events: write # required for uploading code scanning alerts
    # mozilla/ssdlc-actions@1f6499e — first commit with the Semgrep job (PR #22)
    uses: mozilla/ssdlc-actions/.github/workflows/code-security-analysis-public-repo.yml@1f6499e564c784352777377dc3c1a9467fd1159d
